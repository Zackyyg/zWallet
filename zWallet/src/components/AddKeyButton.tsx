import './AddKeyButton.css';

function AddKeyButton() {
    return (
        <div className="AddButtonItem">
            <button className="AddButton">
              Add a new Key
            </button>
      </div>
    );
}

export default AddKeyButton
